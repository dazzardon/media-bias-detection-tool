# Streamlit and basic dependencies
streamlit==1.10.0  # Adjust as needed for compatibility
setuptools  # Includes distutils

# Core Python packages with broader compatibility
numpy==1.19.5
pandas==1.1.5
scikit-learn==0.24.2

# Torch and NLP dependencies with compatible versions
torch==1.9.1
sentencepiece==0.1.91
tokenizers==0.10.3
transformers==4.11.0  # Ensure compatibility with the above packages

# Additional libraries if used in your app
requests==2.25.1
beautifulsoup4==4.9.3  # If HTML parsing is required
